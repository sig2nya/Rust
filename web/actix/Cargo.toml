[package]
name = "actix"
version = "0.1.0"
edition = "2024"
default-run = "actix"

[dependencies]
actix-web = "4.9.0"
actix-rt = "2.10.0"

serde = {version = "1.0.209", features = ["derive"]}
chrono = {version = "0.4.38", features = ["serde"]}
